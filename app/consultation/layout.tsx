export const metadata = {
  title: "Request a Consultation — Velvet Concierge Care",
  description: "Tell us about your loved one so we can prepare a meaningful first conversation.",
};

export default function ConsultationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
